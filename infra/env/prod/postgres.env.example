# Legacy only: current production compose stack does not run Postgres.
# Kept only for emergency self-hosted Postgres fallback workflows.
POSTGRES_DB=bominal
POSTGRES_USER=bominal
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
