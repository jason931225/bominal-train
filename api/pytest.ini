[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
# Temporary relaxation during governance/workflow overhaul:
# no repository-level backend coverage gate is enforced from pytest config.
addopts = -q
