---
phase: 09-css-and-build-pipeline
plan: 01
type: execute
wave: 1
requirements:
  - CSS-01
  - CSS-02
files_modified:
  - Cargo.toml
  - crates/bominal-app/style/app.css
  - tailwind.config.js
  - dev-build.sh
  - Makefile
---

# Phase 9 / Plan 01

## Objective

Make the local CSS/build workflow use cargo-leptos and the active `bominal-app` stylesheet instead of the deleted `bominal-frontend` toolchain.

## Implementation

1. Remove stale Tailwind JS config usage from the cargo-leptos metadata and repo surface.
2. Update the active app stylesheet to the Tailwind v4 import/source contract scoped to Leptos `.rs` sources.
3. Replace local build helper surfaces (`dev-build.sh`, `Makefile`) with cargo-leptos-based commands for build/watch/serve flows.

## Verification

- `cargo leptos build`
- `cargo check -p bominal-app --features ssr`
- `cargo check -p bominal-app --target wasm32-unknown-unknown --features hydrate`

## Output

Create `.planning/phases/09-css-and-build-pipeline/09-01-SUMMARY.md`.
