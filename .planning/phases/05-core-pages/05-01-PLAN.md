---
phase: 05-core-pages
plan: 01
type: execute
wave: 1
requirements:
  - PAGE-01
files_modified:
  - crates/bominal-app/src/components/mod.rs
  - crates/bominal-app/src/components/sse_reload.rs
  - crates/bominal-app/src/components/status_chip.rs
  - crates/bominal-app/src/pages/mod.rs
  - crates/bominal-app/src/pages/home.rs
  - crates/bominal-app/src/lib.rs
  - crates/bominal-app/style/app.css
---

# Phase 5 / Plan 01

## Objective

Replace the home stub with a live dashboard and land the minimal shared helpers needed for the rest of the core pages.

## Implementation

1. Add the small shared helpers Phase 5 needs in `bominal-app`: SSE reload wiring and a reusable status badge.
2. Create `pages/home.rs` with an auth-guarded dashboard that shows active task summaries, quick links, and a refresh affordance.
3. Repoint the `/home` route in `lib.rs` and add any supporting page-shell styles in `app.css`.

## Verification

- `cargo test -p bominal-app --lib`
- `cargo check -p bominal-app --features ssr`
- `cargo check -p bominal-app --target wasm32-unknown-unknown --features hydrate`

## Output

Create `.planning/phases/05-core-pages/05-01-SUMMARY.md`.
