---
phase: 05-core-pages
plan: 04
type: execute
wave: 1
requirements:
  - PAGE-04
files_modified:
  - crates/bominal-app/src/pages/reservations.rs
  - crates/bominal-app/src/pages/mod.rs
  - crates/bominal-app/src/lib.rs
  - crates/bominal-app/style/app.css
---

# Phase 5 / Plan 04

## Objective

Replace the reservations stub with a working reservation list that supports provider filtering, ticket detail expansion, and pay/cancel/refund actions.

## Implementation

1. Build the reservations page around the typed reservation and card APIs already exposed in `crate::api`.
2. Keep the provider filter, feedback banners, and ticket-detail expansion local to the page instead of porting the donor card abstractions wholesale.
3. Wire the action success paths to refetch reservation data so the UI reflects payment/cancel/refund mutations immediately.

## Verification

- `cargo test -p bominal-app --lib`
- `cargo check -p bominal-app --features ssr`
- `cargo check -p bominal-app --target wasm32-unknown-unknown --features hydrate`

## Output

Create `.planning/phases/05-core-pages/05-04-SUMMARY.md`.
