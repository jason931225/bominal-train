---
phase: 05-core-pages
plan: 03
type: execute
wave: 1
requirements:
  - PAGE-03
files_modified:
  - crates/bominal-app/src/pages/tasks.rs
  - crates/bominal-app/src/pages/mod.rs
  - crates/bominal-app/src/lib.rs
  - crates/bominal-app/style/app.css
---

# Phase 5 / Plan 03

## Objective

Replace the tasks stub with the live task-monitoring experience, including active/completed tabs, SSE updates, and swipe-reveal cancellation.

## Implementation

1. Build the tasks page around `list_tasks`, `update_task`, and `delete_task`.
2. Segment active vs completed tasks in-page, keeping the current shell navigation contract intact.
3. Add a mobile-friendly swipe-reveal action row for cancellation while leaving the interaction accessible on desktop.

## Verification

- `cargo test -p bominal-app --lib`
- `cargo check -p bominal-app --features ssr`
- `cargo check -p bominal-app --target wasm32-unknown-unknown --features hydrate`

## Output

Create `.planning/phases/05-core-pages/05-03-SUMMARY.md`.
