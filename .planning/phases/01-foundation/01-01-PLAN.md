---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/api/search.ts
  - frontend/src/routes/search/+page.svelte
autonomous: true
requirements:
  - SRCH-07
  - SRCH-06

must_haves:
  truths:
    - "Station suggest API returns results when user types a station name (query parameter matches backend expectation)"
    - "Search button enables only when departure, arrival, and date fields are all populated"
  artifacts:
    - path: "frontend/src/lib/api/search.ts"
      provides: "suggestStations() sends q parameter matching backend SuggestQuery struct"
      contains: "q: query"
    - path: "frontend/src/routes/search/+page.svelte"
      provides: "Search button disabled until all three fields populated"
      contains: "!date"
  key_links:
    - from: "frontend/src/lib/api/search.ts"
      to: "/api/stations/{provider}/suggest"
      via: "get() call with params object where key q maps to URLSearchParams q= in request"
      pattern: "q: query"
    - from: "frontend/src/routes/search/+page.svelte"
      to: "handleSearch"
      via: "disabled attribute gates button click"
      pattern: "!departure.*!arrival.*!date"
---

<objective>
Fix two blocking bugs in the search form that prevent basic functionality from working.

Purpose: The station suggest API returns no results because the frontend sends the wrong query parameter name, and the search button enables before the user picks a date. Both must work before any Phase 2+ features can build on top.
Output: Two corrected lines in the SvelteKit frontend -- no new files, no backend changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Backend contract the frontend must match -->

From crates/bominal-server/src/search.rs (lines 85-90):
```rust
#[derive(Deserialize)]
pub struct SuggestQuery {
    pub q: String,
    pub mode: Option<String>,
}
```

From frontend/src/lib/api/client.ts:
```typescript
// get<T>(path, params?) converts params Record<string, string> keys
// directly into URLSearchParams keys — so the key in the object
// MUST match the backend's deserialized struct field name exactly.
export function get<T>(path: string, params?: Record<string, string>): Promise<T>;
```

From frontend/src/routes/search/+page.svelte (line 36):
```svelte
let date = $state('');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix suggest API parameter key (SRCH-07)</name>
  <files>frontend/src/lib/api/search.ts</files>
  <read_first>
    - frontend/src/lib/api/search.ts (current suggestStations function, see line 23)
    - crates/bominal-server/src/search.rs (SuggestQuery struct with field `q`, lines 85-90)
  </read_first>
  <action>
In `frontend/src/lib/api/search.ts`, line 23, change the params object key from `query` to `q: query` per D-01.

Before:
```typescript
const params: Record&lt;string, string&gt; = { query };
```

After:
```typescript
const params: Record&lt;string, string&gt; = { q: query };
```

This uses JavaScript's property shorthand rename syntax so the URLSearchParams key becomes `q` (matching backend `SuggestQuery.q`) while keeping the descriptive `query` parameter name in the function signature.

Do NOT modify any backend files. The backend `SuggestQuery` struct is the source of truth (per D-01).
  </action>
  <verify>
    <automated>cd /Users/jasonlee/projects/bominal-train && grep -n 'q: query' frontend/src/lib/api/search.ts && cargo test -p bominal-server -- suggest</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/lib/api/search.ts line 23 contains `{ q: query }` (verified by: grep -n "q: query" frontend/src/lib/api/search.ts)
    - frontend/src/lib/api/search.ts does NOT contain `{ query }` as a standalone params value (verified by: grep -c "= { query }" frontend/src/lib/api/search.ts returns 0)
    - Backend suggest tests still pass (verified by: cargo test -p bominal-server -- suggest exits 0)
    - No changes to crates/bominal-server/src/search.rs (verified by: git diff crates/bominal-server/src/search.rs shows no diff)
  </acceptance_criteria>
  <done>suggestStations() sends `q` as the URL parameter key, matching the backend SuggestQuery struct. Backend tests confirm no regression.</done>
</task>

<task type="auto">
  <name>Task 2: Fix search button disabled state (SRCH-06)</name>
  <files>frontend/src/routes/search/+page.svelte</files>
  <read_first>
    - frontend/src/routes/search/+page.svelte (search button disabled attribute at line 526, date state declaration at line 36)
  </read_first>
  <action>
In `frontend/src/routes/search/+page.svelte`, line 526, add `|| !date` to the disabled attribute expression per D-02.

Before:
```svelte
disabled={searching || !departure || !arrival}
```

After:
```svelte
disabled={searching || !departure || !arrival || !date}
```

The `date` variable is declared as `let date = $state('')` at line 36. Since empty string is falsy in JavaScript, `!date` correctly evaluates to `true` when no date is selected, keeping the button disabled.

Do NOT extract this into a `$derived` variable -- the inline expression is clear enough for a boolean OR chain.
  </action>
  <verify>
    <automated>cd /Users/jasonlee/projects/bominal-train && grep -n '!date' frontend/src/routes/search/+page.svelte && cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/routes/search/+page.svelte contains `!departure || !arrival || !date` on the disabled attribute line (verified by: grep "!departure || !arrival || !date" frontend/src/routes/search/+page.svelte)
    - Frontend builds successfully (verified by: cd frontend && npm run build exits 0)
    - The disabled attribute line does NOT contain the old pattern without date (verified by: grep -c "searching || !departure || !arrival}" frontend/src/routes/search/+page.svelte returns 0)
  </acceptance_criteria>
  <done>Search button remains disabled until departure, arrival, AND date are all populated. Frontend builds cleanly.</done>
</task>

</tasks>

<verification>
1. `grep "q: query" frontend/src/lib/api/search.ts` -- confirms SRCH-07 fix
2. `grep "!date" frontend/src/routes/search/+page.svelte` -- confirms SRCH-06 fix
3. `cargo test -p bominal-server -- suggest` -- no backend regression
4. `cd frontend && npm run build` -- frontend compiles without errors
</verification>

<success_criteria>
- Station suggest API calls from the frontend use `?q=...` parameter (matching backend SuggestQuery.q)
- Search button disabled attribute includes `!date` alongside `!departure` and `!arrival`
- All existing backend tests pass
- Frontend builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
