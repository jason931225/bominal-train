---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/bominal-app/Cargo.toml
  - crates/bominal-server/Cargo.toml
autonomous: true
requirements:
  - FND-01
  - FND-02
  - FND-03

must_haves:
  truths:
    - "The root workspace declares a cargo-leptos multi-package project via [[workspace.metadata.leptos]] targeting bominal-server and bominal-app"
    - "bominal-app exposes explicit ssr and hydrate features with islands enabled instead of csr"
    - "bominal-app depends on bominal-ui as the canonical shared UI source"
    - "bominal-server exposes an ssr build path that cargo-leptos can target"
    - "crates/bominal-frontend is no longer treated as an active build target in Phase 1"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace Leptos dependencies and cargo-leptos workspace metadata"
      contains: "workspace.metadata.leptos"
    - path: "crates/bominal-app/Cargo.toml"
      provides: "Feature-gated islands app manifest"
      contains: "bominal-ui"
    - path: "crates/bominal-server/Cargo.toml"
      provides: "Server-side ssr feature path for cargo-leptos"
      contains: "ssr ="
  key_links:
    - from: "Cargo.toml"
      to: "crates/bominal-app/Cargo.toml"
      via: "lib-package = bominal-app in workspace metadata"
      pattern: "lib-package.*bominal-app"
    - from: "Cargo.toml"
      to: "crates/bominal-server/Cargo.toml"
      via: "bin-package = bominal-server in workspace metadata"
      pattern: "bin-package.*bominal-server"
---

<objective>
Converge the repo onto one active Leptos app crate and establish the feature-gated build graph required for the migration foundation.

Purpose: Before any pages can be ported, the workspace must agree on which crate is the real frontend, how SSR and hydration are selected, and how cargo-leptos targets the server and app crates together.
Output: Root workspace metadata for cargo-leptos, a rewritten `bominal-app` manifest for islands/ssr/hydrate, and a server manifest with an `ssr` feature path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

@Cargo.toml
@crates/bominal-app/Cargo.toml
@crates/bominal-server/Cargo.toml
@crates/bominal-frontend/Cargo.toml
@../bominal-ui/Cargo.toml
@../bominal-ui/src/css/mod.rs

<interfaces>
From cargo-leptos workspace docs:
```toml
[[workspace.metadata.leptos]]
name = "leptos-project"
bin-package = "server"
lib-package = "front"
```

From Leptos islands docs:
```rust
leptos::mount::hydrate_islands();
```

Current constraints from the repo:
- `crates/bominal-app` is the only app crate already in the workspace
- `crates/bominal-frontend` expects workspace `leptos*` dependencies that do not exist yet
- `crates/bominal-server` currently has no `ssr` feature at all
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace Leptos dependencies and cargo-leptos workspace metadata</name>
  <files>Cargo.toml</files>
  <action>
    Update the root `Cargo.toml` to make the workspace a real cargo-leptos multi-package project.

    1. Add workspace-scoped Leptos dependencies for the versions aligned with the current migration (`leptos`, `leptos_meta`, `leptos_router`, `leptos_axum`). Use feature-neutral workspace dependencies where possible so the crate-level manifests control `ssr`, `hydrate`, and `islands`.
    2. Add `bominal-ui = { path = "../bominal-ui", default-features = false }` to shared dependencies so the active app can consume the canonical shared UI crate.
    3. Add a `[[workspace.metadata.leptos]]` block with:
       - `name = "bominal-train"`
       - `bin-package = "bominal-server"`
       - `lib-package = "bominal-app"`
       - `output-name = "bominal-app"`
       - `site-root = "target/site"`
       - `site-pkg-dir = "pkg"`
       - `style-file = "crates/bominal-app/style/app.css"`
       - `tailwind-input-file = "crates/bominal-app/style/app.css"`
       - `tailwind-config-file = "tailwind.config.js"`
       - `assets-dir = "crates/bominal-app/assets"`
       - `bin-features = ["ssr"]`
       - `bin-default-features = false`
       - `lib-features = ["hydrate"]`
       - `lib-default-features = false`

    Do NOT create `Cargo-leptos.toml`. The current workspace docs place this configuration in the root manifest.
  </action>
  <verify>
    <automated>cd /Users/jasonlee/projects/bominal-train && rg -n "workspace.metadata.leptos|bin-package|lib-package|tailwind-input-file" Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` contains `[[workspace.metadata.leptos]]`
    - `Cargo.toml` names `bominal-server` as `bin-package`
    - `Cargo.toml` names `bominal-app` as `lib-package`
    - Root workspace dependencies include the Leptos stack needed by both the server and app crates
  </acceptance_criteria>
  <done>
    cargo-leptos can discover the repo as a workspace Leptos project and target the intended server/app crates.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the active app/server manifests for islands-based feature gating</name>
  <files>crates/bominal-app/Cargo.toml, crates/bominal-server/Cargo.toml</files>
  <action>
    Rewrite the manifests so the workspace has one active app crate and a valid server `ssr` feature path.

    **In `crates/bominal-app/Cargo.toml`:**
    1. Remove the CSR-only dependency shape (`features = ["csr"]`, `gloo-net`).
    2. Keep `crate-type = ["cdylib", "rlib"]`.
    3. Use workspace Leptos dependencies and enable `features = ["islands"]` on the `leptos` dependency.
    4. Add `bominal-ui` as a dependency with the feature set needed by this product (`leptos`, `train`, `auth`).
    5. Add explicit feature gates:
       - `default = []`
       - `ssr = [...]`
       - `hydrate = [...]`
    6. Keep only dependencies that make sense for the active app crate's foundation state. Browser-only/WASM dependencies stay allowed, but they must not leak into the SSR path.

    **In `crates/bominal-server/Cargo.toml`:**
    1. Add a `[features]` section with `default = []` and `ssr = [...]`.
    2. Add any optional dependencies needed so the server crate can legitimately compile with `--features ssr` in the cargo-leptos build path.
    3. Do NOT replace the SvelteKit fallback routes yet. This is only build-path plumbing.

    **Ownership rule for this phase:**
    - `crates/bominal-frontend` stays outside the active workspace path and is not promoted back into `members`.
    - Do not delete its source tree in this plan. It is donor code until later cleanup.
    - `bominal-ui` is the design-system source of truth; do not preserve train-local CSS as the canonical path in the app manifest.
  </action>
  <verify>
    <automated>cd /Users/jasonlee/projects/bominal-train && cargo check -p bominal-app --features ssr && cargo check -p bominal-app --target wasm32-unknown-unknown --features hydrate && cargo check -p bominal-server --features ssr</automated>
  </verify>
  <acceptance_criteria>
    - `crates/bominal-app/Cargo.toml` has explicit `ssr` and `hydrate` features
    - `crates/bominal-app/Cargo.toml` depends on `bominal-ui`
    - `crates/bominal-app/Cargo.toml` enables Leptos islands mode
    - `crates/bominal-app/Cargo.toml` no longer depends on the CSR-only `gloo-net` path
    - `crates/bominal-server/Cargo.toml` defines an `ssr` feature
    - All three verification commands exit successfully
  </acceptance_criteria>
  <done>
    The workspace has one active app crate with explicit SSR/hydrate feature gates and a server binary that cargo-leptos can target with `--features ssr`.
  </done>
</task>

</tasks>

<verification>
1. `rg "workspace.metadata.leptos|bin-package|lib-package" Cargo.toml`
2. `cargo check -p bominal-app --features ssr`
3. `cargo check -p bominal-app --target wasm32-unknown-unknown --features hydrate`
4. `cargo check -p bominal-server --features ssr`
</verification>

<success_criteria>
- Root workspace metadata declares the Leptos multi-package project
- `bominal-app` is converted from CSR-only assumptions to explicit islands/ssr/hydrate feature gates
- `bominal-app` is wired to the canonical `bominal-ui` crate
- `bominal-server` exposes a valid `ssr` build path
- The dormant `bominal-frontend` crate is no longer treated as the active migration target
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
