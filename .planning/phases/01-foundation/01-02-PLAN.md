---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - crates/bominal-app/src/lib.rs
  - crates/bominal-app/src/i18n.rs
  - crates/bominal-app/style/app.css
  - tailwind.config.js
  - crates/bominal-app/assets/.gitkeep
autonomous: true
requirements:
  - FND-04
  - FND-05

must_haves:
  truths:
    - "bominal-app exports a minimal SSR-safe App and a hydrate entrypoint that uses leptos::mount::hydrate_islands()"
    - "Browser-only CSR modules no longer block ssr or hydrate compilation"
    - "The active app crate consumes or deterministically derives its CSS inputs from bominal-ui"
    - "cargo leptos build succeeds against the workspace metadata added in plan 01-01"
  artifacts:
    - path: "crates/bominal-app/src/lib.rs"
      provides: "Minimal islands-ready app entrypoint"
      contains: "hydrate_islands"
    - path: "crates/bominal-app/style/app.css"
      provides: "Active app stylesheet entrypoint derived from bominal-ui"
      contains: "--lg-"
    - path: "tailwind.config.js"
      provides: "Tailwind scan config for Rust view! files"
      contains: "crates/bominal-app/src/**/*.rs"
  key_links:
    - from: "Cargo.toml"
      to: "crates/bominal-app/style/app.css"
      via: "workspace metadata style-file/tailwind-input-file"
      pattern: "style-file.*crates/bominal-app/style/app.css"
    - from: "crates/bominal-app/src/lib.rs"
      to: "leptos islands runtime"
      via: "hydrate entrypoint"
      pattern: "hydrate_islands"
---

<objective>
Replace the current CSR-only app entrypoint with a minimal islands-ready foundation and validate the full cargo-leptos build path.

Purpose: After the manifests are corrected in plan 01-01, the app crate still needs an SSR-safe entrypoint and a style pipeline that cargo-leptos can actually build. This plan makes the build graph real.
Output: Minimal `bominal-app` shell, SSR-safe i18n baseline, `bominal-ui`-backed style asset, Tailwind scan config, and successful `cargo leptos build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-PLAN.md

@crates/bominal-app/src/lib.rs
@crates/bominal-app/src/i18n.rs
@../bominal-ui/src/css/mod.rs
@../bominal-ui/src/css/ecosystem.rs
@../bominal-ui/src/css/train.rs
@../bominal-ui/src/css/auth.rs
@frontend/src/app.css
@crates/bominal-frontend/src/hydrate.rs

<interfaces>
From the Leptos islands guide:
```rust
leptos::mount::hydrate_islands();
```

Current repo reality:
- `crates/bominal-app/src/lib.rs` uses `#[wasm_bindgen(start)]` + `mount_to_body(App)`
- `crates/bominal-app/src/i18n.rs` reads from `window.navigator`
- `crates/bominal-frontend/src/hydrate.rs` still uses `hydrate_body(App)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the CSR-only app entrypoint with a minimal islands-ready shell</name>
  <files>crates/bominal-app/src/lib.rs, crates/bominal-app/src/i18n.rs</files>
  <action>
    Make `bominal-app` compile cleanly in both SSR and hydrate modes, even if it means temporarily shrinking the visible app surface.

    1. Rewrite `crates/bominal-app/src/lib.rs` to export a minimal `App` component that is safe for both server and client compilation.
    2. Remove the `#[wasm_bindgen(start)]` + `mount_to_body(App)` pattern and replace the hydrate entrypoint with `leptos::mount::hydrate_islands()` behind `#[cfg(feature = "hydrate")]`.
    3. Stop compiling the current CSR-only modules (`api`, `pages`, and any browser-dependent auth wiring) through the default library path. Keep source files on disk if helpful, but do not let them block `ssr` or `hydrate` compilation in Phase 1.
    4. Replace the current browser-only locale detection in `crates/bominal-app/src/i18n.rs` with an SSR-safe temporary baseline. It is acceptable for Phase 1 to use a simple fallback implementation as long as it does not reintroduce browser-only assumptions into the server build.

    Important:
    - This is a foundation plan, not a page-port plan.
    - Do not re-implement auth or API behavior here.
  </action>
  <verify>
    <automated>cd /Users/jasonlee/projects/bominal-train && cargo check -p bominal-app --features ssr && cargo check -p bominal-app --target wasm32-unknown-unknown --features hydrate</automated>
  </verify>
  <acceptance_criteria>
    - `crates/bominal-app/src/lib.rs` contains a hydrate entrypoint using `hydrate_islands`
    - `crates/bominal-app/src/lib.rs` no longer uses `#[wasm_bindgen(start)]`
    - `crates/bominal-app/src/lib.rs` no longer calls `mount_to_body(App)`
    - `cargo check -p bominal-app --features ssr` succeeds
    - `cargo check -p bominal-app --target wasm32-unknown-unknown --features hydrate` succeeds
  </acceptance_criteria>
  <done>
    The active app crate has a minimal islands-ready entrypoint and no longer depends on CSR-only startup assumptions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move the shared UI style pipeline into the active app crate and validate cargo-leptos end-to-end</name>
  <files>crates/bominal-app/style/app.css, tailwind.config.js, crates/bominal-app/assets/.gitkeep</files>
  <action>
    Wire the CSS/Tailwind inputs into the active app crate so the cargo-leptos workspace config from plan 01-01 becomes executable without creating a second design-system owner.

    1. Create `crates/bominal-app/style/`.
    2. Create `crates/bominal-app/style/app.css` from the canonical `bominal-ui` CSS payload:
       - ecosystem base
       - `train` product skin
       - `auth` product skin
    3. Make it explicit in comments or generation notes that this file is derived from `bominal-ui`, not hand-owned train-local CSS.
    4. Use `frontend/src/app.css` only as a compatibility reference if class aliases or utility wrappers need bridging.
    5. Create `tailwind.config.js` at the repo root scanning `./crates/bominal-app/src/**/*.rs`.
    6. Create `crates/bominal-app/assets/.gitkeep`.
    7. Run `cargo leptos build`.

    Scope guardrail:
    - Do NOT update `dev-build.sh`, `Dockerfile`, or deployment scripts here. They remain later-phase work.
    - Do NOT try to match the final visual fidelity in this plan. The goal is build-path validity, not a completed design port.
    - Do NOT create a second long-lived hand-maintained token file that drifts from `bominal-ui`.
  </action>
  <verify>
    <automated>cd /Users/jasonlee/projects/bominal-train && cargo leptos build && test -d target/site/pkg && ls target/site/pkg</automated>
  </verify>
  <acceptance_criteria>
    - `crates/bominal-app/style/app.css` exists
    - `crates/bominal-app/style/app.css` is explicitly derived from `bominal-ui`
    - `tailwind.config.js` scans `crates/bominal-app/src/**/*.rs`
    - `cargo leptos build` succeeds
    - `target/site/pkg` exists after the build
  </acceptance_criteria>
  <done>
    The active app crate has a `bominal-ui`-backed CSS input and the repo has a real, end-to-end cargo-leptos build path.
  </done>
</task>

</tasks>

<verification>
1. `cargo check -p bominal-app --features ssr`
2. `cargo check -p bominal-app --target wasm32-unknown-unknown --features hydrate`
3. `cargo leptos build`
4. `test -d target/site/pkg`
</verification>

<success_criteria>
- `bominal-app` exports a minimal islands-ready app entrypoint
- Browser-only CSR code no longer blocks SSR or hydrate compilation
- The style pipeline is owned by the active app crate but sourced from `bominal-ui`
- `cargo leptos build` succeeds with the workspace metadata introduced in plan 01-01
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
