---
phase: 08-server-integration
plan: 02
type: execute
wave: 1
requirements:
  - SRV-03
files_modified:
  - crates/bominal-server/src/routes.rs
  - crates/bominal-server/src/state.rs
  - crates/bominal-app/src/api.rs
  - crates/bominal-app/src/lib.rs
  - crates/bominal-server/tests/common/mod.rs
---

# Phase 8 / Plan 02

## Objective

Unify the existing shared server state with the Leptos SSR/server-function context so rendering and server actions operate against the real application dependencies.

## Implementation

1. Provide the existing `SharedState` into the Leptos SSR request path rather than building a second state container.
2. Ensure server functions can access the dependencies they need through the merged state/context contract.
3. Keep the test harness aligned with the new production state shape and verify the server crate compiles with SSR enabled.

## Verification

- `cargo test -p bominal-app --lib`
- `cargo check -p bominal-app --features ssr`
- `cargo check -p bominal-app --target wasm32-unknown-unknown --features hydrate`
- `cargo check -p bominal-server --features ssr`

## Output

Create `.planning/phases/08-server-integration/08-02-SUMMARY.md`.
