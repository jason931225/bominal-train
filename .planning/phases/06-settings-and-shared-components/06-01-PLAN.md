---
phase: 06-settings-and-shared-components
plan: 01
type: execute
wave: 1
requirements:
  - SETT-01
files_modified:
  - crates/bominal-app/src/pages/settings.rs
  - crates/bominal-app/src/pages/mod.rs
  - crates/bominal-app/src/lib.rs
  - crates/bominal-app/style/app.css
---

# Phase 6 / Plan 01

## Objective

Replace the `/settings` stub with a working account/settings page covering providers, cards, appearance, and logout.

## Implementation

1. Port the donor settings-page section structure into `bominal-app/src/pages/settings.rs`, adapting it to the current typed `/api` proxy layer.
2. Hook provider management, card management, appearance toggles, and logout into the existing app-local browser/state helpers.
3. Repoint the `/settings` route in `lib.rs` and add the small amount of styling needed for the section shells.

## Verification

- `cargo test -p bominal-app --lib`
- `cargo check -p bominal-app --features ssr`
- `cargo check -p bominal-app --target wasm32-unknown-unknown --features hydrate`

## Output

Create `.planning/phases/06-settings-and-shared-components/06-01-SUMMARY.md`.
