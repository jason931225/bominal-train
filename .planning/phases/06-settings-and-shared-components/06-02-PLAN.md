---
phase: 06-settings-and-shared-components
plan: 02
type: execute
wave: 1
requirements:
  - COMP-01
  - COMP-02
files_modified:
  - crates/bominal-app/src/components/*.rs
  - crates/bominal-app/src/pages/settings.rs
  - crates/bominal-app/src/pages/tasks.rs
  - crates/bominal-app/src/pages/reservations.rs
  - crates/bominal-app/style/app.css
---

# Phase 6 / Plan 02

## Objective

Port and normalize the remaining shared SSR and island components needed by settings and later phases.

## Implementation

1. Land the pure SSR shared helpers first (`GlassPanel`, `Skeleton`, `Icon`, `CardBrand`, and the normalized `StatusChip` surface).
2. Port the interactive island components (`BottomSheet`, `SelectionPrompt`, `TicketCard`, `TaskCard`) in app-local form, keeping their behavior compatible with the existing Phase 5 pages.
3. Refit the settings/reservation/task surfaces onto those shared components where the reuse is clear and reduces duplication.

## Verification

- `cargo test -p bominal-app --lib`
- `cargo check -p bominal-app --features ssr`
- `cargo check -p bominal-app --target wasm32-unknown-unknown --features hydrate`

## Output

Create `.planning/phases/06-settings-and-shared-components/06-02-SUMMARY.md`.
