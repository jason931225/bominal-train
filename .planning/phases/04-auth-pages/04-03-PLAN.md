---
phase: 04-auth-pages
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
requirements:
  - AUTH-01
  - AUTH-05
files_modified:
  - crates/bominal-app/src/api/passkey.rs
  - crates/bominal-app/src/api.rs
  - crates/bominal-app/src/pages/auth/mod.rs
  - crates/bominal-app/src/pages/auth/add_passkey.rs
  - crates/bominal-app/src/lib.rs
  - crates/bominal-app/Cargo.toml
---

# Phase 4 / Plan 03

## Objective

Bring in the minimum passkey WASM client needed for the auth landing and add-passkey flows so the pages can trigger live browser credential ceremonies.

## Implementation

1. Adapt the donor `api/passkey.rs` client into `bominal-app`, keeping it scoped to the existing `/api/auth/passkey/...` endpoints.
2. Wire the landing and add-passkey pages to that client and ensure success/error behavior updates the current auth route flow cleanly.
3. Keep the implementation intentionally narrow so it satisfies Phase 4 page requirements without absorbing all broader Phase 7 interop concerns.

## Verification

- `cargo test -p bominal-app --lib`
- `cargo check -p bominal-app --features ssr`
- `cargo check -p bominal-app --target wasm32-unknown-unknown --features hydrate`

## Output

Create `.planning/phases/04-auth-pages/04-03-SUMMARY.md`.
