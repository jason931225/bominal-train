---
phase: 04-auth-pages
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
requirements:
  - AUTH-05
  - AUTH-06
files_modified:
  - crates/bominal-app/src/pages/auth/mod.rs
  - crates/bominal-app/src/pages/auth/verify.rs
  - crates/bominal-app/src/pages/auth/add_passkey.rs
  - crates/bominal-app/src/pages/verify_email.rs
  - crates/bominal-app/src/pages/reset_password.rs
  - crates/bominal-app/src/pages/mod.rs
  - crates/bominal-app/src/shell_pages.rs
---

# Phase 4 / Plan 02

## Objective

Port the remaining verify/add-passkey/email-token auth routes and adapt them to the app-local API/state surface.

## Implementation

1. Add the missing app-local auth pages for `/auth/verify` and `/auth/add-passkey`, borrowing layout and behavior from the donor Leptos crate.
2. Add standalone token-driven pages for `/verify-email` and `/reset-password`, adapting the donor query-param flows to `crate::api`.
3. Route all Phase 4 auth/email paths through real page modules instead of shell stubs.

## Verification

- `cargo test -p bominal-app --lib`
- `cargo check -p bominal-app --features ssr`
- `cargo check -p bominal-app --target wasm32-unknown-unknown --features hydrate`

## Output

Create `.planning/phases/04-auth-pages/04-02-SUMMARY.md`.
