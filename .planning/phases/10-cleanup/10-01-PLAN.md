---
phase: 10-cleanup
plan: 01
type: execute
wave: 1
requirements:
  - CLEAN-01
  - CLEAN-02
files_modified:
  - CLAUDE.md
  - crates/bominal-service/src/lib.rs
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - docs/WASM_HYDRATION_PLAN.md
  - docs/superpowers/specs/2026-03-12-frontend-rewrite-design.md
  - frontend/
  - crates/bominal-frontend/
  - crates/bominal-app/index.html
  - crates/bominal-app/style/main.css
---

# Phase 10 / Plan 01

## Objective

Remove the obsolete frontend trees and repo guidance that still describe the pre-migration architecture, then verify the Leptos/cargo-leptos stack stands on its own.

## Implementation

1. Delete `frontend/`, `crates/bominal-frontend/`, and leftover Trunk-era shell files that still depend on deleted styles.
2. Rewrite the repo guide and supporting docs/comments so they describe `bominal-app`, Axum SSR, cargo-leptos, and the named-context Docker build.
3. Re-run the current build checks, measure the final gzipped WASM bundle, and update roadmap/requirements/state to mark the migration complete.

## Verification

- `cargo test -p bominal-app --lib`
- `cargo leptos build --release --frontend-only`
- `cargo check -p bominal-app --features ssr`
- `cargo check -p bominal-app --target wasm32-unknown-unknown --features hydrate`
- `cargo check -p bominal-server --features ssr`
- `bash -n deployment/build.sh && bash -n deployment/bootstrap.sh`
- `rg -n "npm|Node\\.js|frontend/build|bominal-frontend|SvelteKit|vite" CLAUDE.md dev-build.sh deployment/build.sh deployment/bootstrap.sh Dockerfile Cargo.toml crates/bominal-app crates/bominal-server crates/bominal-service docs`

## Output

Create `.planning/phases/10-cleanup/10-01-SUMMARY.md` and `.planning/phases/10-cleanup/10-VERIFICATION.md`.
