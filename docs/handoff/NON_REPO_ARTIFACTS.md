# Non-Repo Artifacts

These artifacts are intentionally not copied into repository docs.

## External Paths

- `/Users/jasonlee/Downloads/flows`
  - Type: user-provided flow dump (outside git workspace)
  - Portability: non-portable (machine-local path)
  - Handling: treat as sensitive; do not paste raw content into docs/issues

## Policy

- `docs/handoff` keeps pointers and redacted summaries only.
- Raw payloads and potentially sensitive captures remain in controlled locations under `tools/srtgo-capture/output` or external local storage.
